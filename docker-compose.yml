
services:
  postgres:
    container_name: etl_example-db
    image: postgres
    ports:
      - 5432:5432
    environment:
       - POSTGRES_PASSWORD=dbpassword
       - POSTGRES_USER=dbuser
       - POSTGRES_DB=postgres
    volumes:
      - ./db:/docker-entrypoint-initdb.d

